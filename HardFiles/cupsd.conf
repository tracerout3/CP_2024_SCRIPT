# Disable remote access, only allow localhost
Listen localhost:631
BrowseRemoteProtocols none

# Restrict access to printers
<Location /admin>
    Require ip 127.0.0.1
</Location>

# Disable unused services
Listen /var/run/cups/cups.sock
